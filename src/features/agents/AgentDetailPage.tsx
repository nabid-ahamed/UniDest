import { Link, useParams } from 'react-router-dom'
import { useAgent, useAgentCommissions, useAgentReferrals, useUpdateAgent } from '../../lib/api'

export default function AgentDetailPage() {
  const id = Number(useParams().id)
  const { data: agent, isLoading } = useAgent(id)
  const { data: referrals = [] } = useAgentReferrals({ agentId: String(id) })
  const { data: commissions = [] } = useAgentCommissions({ agentId: String(id) })
  const update = useUpdateAgent()
  if (isLoading) return <p className="text-sm text-slate-500">Loading agent...</p>
  if (!agent) return <p className="text-sm text-rose-600">Agent not found.</p>
  return <section className="mx-auto max-w-6xl space-y-5"><Link to="/agents" className="text-sm font-semibold text-brand-600 hover:underline">← Back to agents</Link><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="text-2xl font-bold text-slate-900">{agent.name}</h1><p className="mt-1 text-sm text-slate-500">{agent.company || 'Independent partner'}</p></div><div className="flex items-center gap-2"><Link to={`/agents/${id}/edit`} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Edit profile</Link><span className="rounded-md bg-emerald-100 px-2.5 py-1 text-xs font-semibold text-emerald-700">{agent.status}</span></div></div><dl className="mt-6 grid gap-x-6 gap-y-5 border-t border-slate-100 pt-5 sm:grid-cols-2 lg:grid-cols-4">{[['First name', agent.firstName], ['Last name', agent.lastName], ['Email', agent.email], ['Mobile', agent.phone], ['Country', agent.country], ['State', agent.state], ['City', agent.city], ['Category', agent.category], ['Address', agent.address], ['Branch ID', agent.branchId], ['Point of contact ID', agent.pointOfContactId ?? 'Not assigned'], ['Commission rate', `${agent.commissionRate}%`], ['Joined', agent.joined], ['Portal account', agent.userId ? 'Linked' : 'Not invited']].map(([label, value]) => <div key={label as string}><dt className="text-xs text-slate-500">{label as string}</dt><dd className="mt-1 break-words text-sm font-semibold text-slate-800">{value as string}</dd></div>)}</dl><div className="mt-6 space-y-3 border-t border-slate-100 pt-5"><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={agent.canSubmitApplications} onChange={(event) => update.mutate({ id, patch: { canSubmitApplications: event.target.checked } })} /> Allow application submission</label><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={agent.autoConvertReferrals} onChange={(event) => update.mutate({ id, patch: { autoConvertReferrals: event.target.checked } })} /> Auto-convert referrals</label></div></div><div className="grid gap-5 lg:grid-cols-2"><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold text-slate-900">Referrals ({referrals.length})</h2><Link to={`/agents/referrals?agentId=${id}`} className="text-sm font-semibold text-brand-600 hover:underline">View all</Link></div><div className="mt-4 space-y-2">{referrals.slice(0, 5).map((row) => <div key={`${row.type}-${row.id}`} className="flex items-center justify-between border-b border-slate-100 py-2 text-sm"><span className="font-semibold text-slate-700">{row.name}</span><span className="text-slate-500">{row.status}</span></div>)}{referrals.length === 0 && <p className="text-sm text-slate-500">No referrals yet.</p>}</div></div><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold text-slate-900">Commissions ({commissions.length})</h2><Link to={`/agents/invoices?agentId=${id}`} className="text-sm font-semibold text-brand-600 hover:underline">View all</Link></div><div className="mt-4 space-y-2">{commissions.slice(0, 5).map((row) => <div key={row.id} className="flex items-center justify-between border-b border-slate-100 py-2 text-sm"><span className="font-semibold text-slate-700">#{row.id} · {row.student}</span><span className="text-slate-600">{row.currency} {row.amount.toFixed(2)}</span></div>)}{commissions.length === 0 && <p className="text-sm text-slate-500">No commissions yet.</p>}</div></div></div></section>
}