import { useAgentCommissions } from '../../lib/api'

export default function AgentInvoicesPortalPage() {
  const { data = [], isLoading } = useAgentCommissions()
  return <section className="mx-auto max-w-6xl rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><h1 className="text-xl font-bold text-slate-900">Invoices</h1><p className="mt-1 text-sm text-slate-500">Invoices and commission records linked to your applications.</p><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[640px]"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-sm font-semibold text-slate-700"><th className="px-4 py-3">Invoice</th><th className="px-4 py-3">Application</th><th className="px-4 py-3">Student</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{isLoading ? <tr><td colSpan={5} className="px-4 py-12 text-center text-sm text-slate-500">Loading invoices...</td></tr> : data.map((row) => <tr key={row.id} className="border-b border-slate-100 text-sm"><td className="px-4 py-4 font-semibold text-slate-800">#{row.id}</td><td className="px-4 py-4 text-slate-600">#{row.applicationId}</td><td className="px-4 py-4 text-slate-600">{row.student}</td><td className="px-4 py-4 font-semibold text-slate-800">{row.currency} {row.amount.toFixed(2)}</td><td className="px-4 py-4 text-slate-600">{row.status}</td></tr>)}{!isLoading && data.length === 0 && <tr><td colSpan={5} className="px-4 py-12 text-center text-sm text-slate-500">No invoices available yet.</td></tr>}</tbody></table></div></section>
}