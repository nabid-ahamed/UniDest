import { useAgentCommissions } from '../../lib/api'

export default function AgentCommissionPage() {
  const { data = [], isLoading } = useAgentCommissions()
  const total = data.reduce((sum, row) => sum + row.amount, 0)
  const currency = data[0]?.currency ?? 'USD'
  return <section className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-xl font-bold text-slate-900">My Commission</h1><p className="mt-1 text-sm text-slate-500">Your earned commission from referred applications.</p></div><div className="rounded-xl bg-emerald-50 px-5 py-3"><p className="text-xs font-semibold uppercase tracking-wide text-emerald-700">Total earnings</p><p className="mt-1 text-2xl font-bold text-emerald-800">{currency} {total.toFixed(2)}</p></div></div><div className="mt-5 overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[720px]"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-sm font-semibold text-slate-700"><th className="px-4 py-3">Invoice ID</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Application</th><th className="px-4 py-3">Student</th><th className="px-4 py-3">Amount Received</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{isLoading ? <tr><td colSpan={6} className="px-4 py-12 text-center text-sm text-slate-500">Loading commissions...</td></tr> : data.map((row) => <tr key={row.id} className="border-b border-slate-100 text-sm"><td className="px-4 py-4 font-semibold text-slate-800">#{row.id}</td><td className="px-4 py-4 text-slate-600">{row.created}</td><td className="px-4 py-4 text-slate-600">#{row.applicationId}</td><td className="px-4 py-4 text-slate-600">{row.student}<br /><span className="text-xs text-slate-500">{row.studentNo}</span></td><td className="px-4 py-4 font-semibold text-slate-800">{row.currency} {row.amount.toFixed(2)}</td><td className="px-4 py-4 text-slate-600">{row.status}</td></tr>)}{!isLoading && data.length === 0 && <tr><td colSpan={6} className="px-4 py-12 text-center text-sm text-slate-500">No commission records yet.</td></tr>}</tbody></table></div></section>
}